# Plesk "Additional nginx directives" for admin.mastergolfclub.com.
# Serve Angular routes through index.html while leaving real assets untouched.
location / {
    try_files $uri $uri/ /index.html;
}
